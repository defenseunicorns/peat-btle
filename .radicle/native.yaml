# Radicle native CI configuration
# Runs on patch updates and default branch pushes
# hive-btle CI pipeline
shell: |
  set -e
  cargo fmt --check
  cargo clippy -- -D warnings
  cargo test
  cargo check --examples
